import { expect } from 'chai';
import chessNotation from './chess-notation';

describe('chessNotation', () => {
  it('should do something', () => {
    expect(chessNotation()).to.equal('Hello');
    // throw new Error('You Messed up Justin');
  });
});

