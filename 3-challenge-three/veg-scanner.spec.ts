import { expect } from 'chai';
import vegScanner from './veg-scanner';

describe('vegScanner', () => {
  it('should do something', () => {
    expect(vegScanner);
    throw new Error('write me a test :)');
  });
});
