import { expect } from 'chai';
import calculatePath from './calculate-path';

describe('calculatePath', () => {
  it('should do something', () => {
    expect(calculatePath);
    throw new Error('write me a test :)');
  });
});
