import { expect } from 'chai';
import connectedFour from './connected-four';

describe('connectedFour', () => {
  it('should do something', () => {
    expect(connectedFour);
    throw new Error('write me a test :)');
  });
});
